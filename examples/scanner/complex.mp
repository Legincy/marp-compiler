calculate(a: int, b: int, c: int) -> int {
    return a * b + c
}

formula(x: int, y: int, z: int) -> int {
    return x + y * z
}

nested(a: int, b: int) -> int {
    return multiply(a, b) + divide(a, b)
}